import React from 'react';
import ReactDom from 'react-dom';
import CostControlApp from './CostControlApp';

const root = document.querySelector( '#root' );

ReactDom.render( <CostControlApp />, root );